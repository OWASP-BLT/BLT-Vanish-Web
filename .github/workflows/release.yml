name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: BLT-Vanish ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: |
            ## BLT-Vanish ${{ github.ref_name }}
            
            ### Downloads
            
            Choose the appropriate version for your platform:
            
            - **Android**: `blt-vanish-android.apk`
            - **Windows**: `blt-vanish-windows-x64.zip`
            - **macOS**: `blt-vanish-macos-x64.zip`
            - **Linux**: `blt-vanish-linux-x64.tar.gz`
            
            ### Verification
            
            All builds include SHA-256 checksums for verification.
            
            ### What's New
            
            See [CHANGELOG.md](https://github.com/OWASP-BLT/BLT-Vanish-Web/blob/main/CHANGELOG.md) for details.
            
            ### Privacy Notice
            
            BLT-Vanish stores all your personal data encrypted on your device only.
            No data is ever transmitted to external servers.
            
            For more information, see our [Security Whitepaper](https://github.com/OWASP-BLT/BLT-Vanish-Web/blob/main/SECURITY.md).

  build-android:
    name: Build Android Release
    runs-on: ubuntu-latest
    needs: create-release
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'
          channel: 'stable'
      
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Get dependencies
        working-directory: flutter_app
        run: flutter pub get
      
      - name: Build APK
        working-directory: flutter_app
        run: flutter build apk --release
      
      - name: Generate checksum
        working-directory: flutter_app/build/app/outputs/flutter-apk
        run: |
          sha256sum app-release.apk > app-release.apk.sha256
      
      - name: Upload APK
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/app/outputs/flutter-apk/app-release.apk
          asset_name: blt-vanish-android.apk
          asset_content_type: application/vnd.android.package-archive
      
      - name: Upload checksum
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/app/outputs/flutter-apk/app-release.apk.sha256
          asset_name: blt-vanish-android.apk.sha256
          asset_content_type: text/plain

  build-windows:
    name: Build Windows Release
    runs-on: windows-latest
    needs: create-release
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'
          channel: 'stable'
      
      - name: Get dependencies
        working-directory: flutter_app
        run: flutter pub get
      
      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop
      
      - name: Build Windows
        working-directory: flutter_app
        run: flutter build windows --release
      
      - name: Create ZIP
        working-directory: flutter_app/build/windows/x64/runner/Release
        run: Compress-Archive -Path * -DestinationPath blt-vanish-windows-x64.zip
      
      - name: Generate checksum
        working-directory: flutter_app/build/windows/x64/runner/Release
        run: |
          $hash = Get-FileHash blt-vanish-windows-x64.zip -Algorithm SHA256
          "$($hash.Hash)  blt-vanish-windows-x64.zip" | Out-File -Encoding ASCII blt-vanish-windows-x64.zip.sha256
      
      - name: Upload Windows ZIP
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/windows/x64/runner/Release/blt-vanish-windows-x64.zip
          asset_name: blt-vanish-windows-x64.zip
          asset_content_type: application/zip
      
      - name: Upload checksum
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/windows/x64/runner/Release/blt-vanish-windows-x64.zip.sha256
          asset_name: blt-vanish-windows-x64.zip.sha256
          asset_content_type: text/plain

  build-macos:
    name: Build macOS Release
    runs-on: macos-latest
    needs: create-release
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'
          channel: 'stable'
      
      - name: Get dependencies
        working-directory: flutter_app
        run: flutter pub get
      
      - name: Enable macOS desktop
        run: flutter config --enable-macos-desktop
      
      - name: Build macOS
        working-directory: flutter_app
        run: flutter build macos --release
      
      - name: Create ZIP
        working-directory: flutter_app/build/macos/Build/Products/Release
        run: zip -r blt-vanish-macos-x64.zip blt-vanish.app
      
      - name: Generate checksum
        working-directory: flutter_app/build/macos/Build/Products/Release
        run: |
          shasum -a 256 blt-vanish-macos-x64.zip > blt-vanish-macos-x64.zip.sha256
      
      - name: Upload macOS ZIP
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/macos/Build/Products/Release/blt-vanish-macos-x64.zip
          asset_name: blt-vanish-macos-x64.zip
          asset_content_type: application/zip
      
      - name: Upload checksum
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/macos/Build/Products/Release/blt-vanish-macos-x64.zip.sha256
          asset_name: blt-vanish-macos-x64.zip.sha256
          asset_content_type: text/plain

  build-linux:
    name: Build Linux Release
    runs-on: ubuntu-latest
    needs: create-release
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'
          channel: 'stable'
      
      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev
      
      - name: Get dependencies
        working-directory: flutter_app
        run: flutter pub get
      
      - name: Enable Linux desktop
        run: flutter config --enable-linux-desktop
      
      - name: Build Linux
        working-directory: flutter_app
        run: flutter build linux --release
      
      - name: Create tar.gz
        working-directory: flutter_app/build/linux/x64/release/bundle
        run: tar -czf blt-vanish-linux-x64.tar.gz *
      
      - name: Generate checksum
        working-directory: flutter_app/build/linux/x64/release/bundle
        run: |
          sha256sum blt-vanish-linux-x64.tar.gz > blt-vanish-linux-x64.tar.gz.sha256
      
      - name: Upload Linux tar.gz
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/linux/x64/release/bundle/blt-vanish-linux-x64.tar.gz
          asset_name: blt-vanish-linux-x64.tar.gz
          asset_content_type: application/gzip
      
      - name: Upload checksum
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: flutter_app/build/linux/x64/release/bundle/blt-vanish-linux-x64.tar.gz.sha256
          asset_name: blt-vanish-linux-x64.tar.gz.sha256
          asset_content_type: text/plain
